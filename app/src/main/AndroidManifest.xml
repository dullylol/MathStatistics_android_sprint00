<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asustuf.sprint00">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_math_stat"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_math_stat_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".task3.Task3Activity"/>
        <activity android:name=".task2.Task2Activity" />
        <activity android:name=".task1.Task1Activity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>